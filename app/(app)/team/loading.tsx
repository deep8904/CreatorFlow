import { PageLoading } from '@/components/dash/PageLoading'

export default function Loading() {
  return <PageLoading eyebrow="Team" title="Team" maxWidth="720px" panelCount={1} panelHeight={320} />
}
