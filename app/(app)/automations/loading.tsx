import { PageLoading } from '@/components/dash/PageLoading'

export default function Loading() {
  return <PageLoading eyebrow="Automations" title="Automations" maxWidth="720px" panelCount={1} panelHeight={420} />
}
