import { PageLoading } from '@/components/dash/PageLoading'

export default function Loading() {
  return <PageLoading eyebrow="Settings" title="Settings" maxWidth="720px" panelCount={3} panelHeight={120} />
}
