import { PageLoading } from '@/components/dash/PageLoading'

export default function Loading() {
  return <PageLoading eyebrow="Ideas" title="Ideas" maxWidth="880px" panelCount={1} panelHeight={400} />
}
