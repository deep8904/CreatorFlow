import { PageLoading } from '@/components/dash/PageLoading'

export default function Loading() {
  return <PageLoading eyebrow="Calendar" title="Calendar" maxWidth="1100px" panelCount={1} panelHeight={480} />
}
